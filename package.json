{
  "name": "readfile-directory-index-fallback",
  "version": "2.0.0-0",
  "description": "fs.readFile using the directory index as a fallback",
  "repository": "shinnn/readfile-directory-index-fallback",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "pretest": "eslint --config @shinnn/node index.js test/test.js",
    "test": "node --strong_mode --harmony_rest_parameters test/test.js | tap-spec",
    "coverage": "node --strong_mode --harmony_rest_parameters node_modules/.bin/istanbul cover --no-default-excludes -x=node_modules/** test/test.js"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "local",
    "read",
    "file",
    "fs",
    "fallback",
    "directory",
    "index",
    "directory-index",
    "asynchronous",
    "buffer"
  ],
  "dependencies": {
    "graceful-fs": "^4.1.2"
  },
  "devDependencies": {
    "@shinnn/eslint-config-node": "^1.0.1",
    "eslint": "^1.6.0",
    "istanbul": "^0.3.22",
    "tap-spec": "^4.1.0",
    "tape": "^4.2.1"
  }
}
